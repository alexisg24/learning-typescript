//.d.ts files are types and interfaces declarations files, not used for code
//Top-level declarations in .d.ts files must start with either a 'declare' or 'export' modifier
export interface AvengerInterface{
    avengerName: string
    avengerPowerScore: number
    avengerWonBattles: number
    avengerAge: number 
}